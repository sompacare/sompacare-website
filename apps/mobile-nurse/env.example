# Copy to apps/mobile-nurse/.env
# Production API (Render)
EXPO_PUBLIC_API_URL=https://sompacare-api.onrender.com/api/v1

# Clerk — same publishable key as web portals
EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_YOUR_KEY

# Local dev only — omit in EAS production builds
# EXPO_PUBLIC_DEV_TOKEN=dev_nurse_rn

# Clerk Dashboard → Native applications → add iOS/Android redirect URLs:
# sompacare-nurse://
